import { MainWalletBase } from '../../bases';
import { EndpointOptions, Wallet } from '../../types';
export declare class IframeWallet extends MainWalletBase {
    constructor(walletInfo: Wallet, preferredEndpoints?: EndpointOptions['endpoints']);
    initClient(): Promise<void>;
}
