import { AminoSignResponse, OfflineAminoSigner } from '@cosmjs/amino';
import { AccountData, DirectSignResponse, OfflineDirectSigner } from '@cosmjs/proto-signing';
export declare class IframeDirectSigner implements OfflineDirectSigner {
    chainId: string;
    constructor(chainId: string);
    getAccounts(...params: any[]): Promise<readonly AccountData[]>;
    signDirect(...params: any[]): Promise<DirectSignResponse>;
}
export declare class IframeAminoSigner implements OfflineAminoSigner {
    chainId: string;
    constructor(chainId: string);
    getAccounts(...params: any[]): Promise<readonly AccountData[]>;
    signAmino(...params: any[]): Promise<AminoSignResponse>;
}
