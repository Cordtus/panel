import { ChainWalletBase } from '../../bases';
import { ChainRecord, Wallet } from '../../types';
export declare class ChainIframe extends ChainWalletBase {
    constructor(walletInfo: Wallet, chainInfo: ChainRecord);
}
